{% load staticfiles %}
<!DOCTYPE html>
<html>
    <head>
        <title>报告预览</title>
        <!-- 引入 Bootstrap -->
        
        <link href="{% static 'reports/css/bootstrap.min.css' %}"" rel="stylesheet">
        <script type="text/javascript" src="{% static 'reports/js/jquery.js' %}"></script>
        <!-- 包括所有已编译的插件 -->
        <script type="text/javascript" src="{% static 'reports/js/bootstrap.min.js' %}"></script>
        <script type="text/javascript" src="{% static 'reports/js/index.js' %}"></script>
    </head>
  <body>
      <div class="container-fluid bg-success" >
          <div class="row">
          <div class="col-lg-12">
            <h1 align="center">联系作者</h1>
            <p align="center">这是一个故障报告生成系统</p>
          </div>
          </div>
                
          <div class="row" id="view-create-button">
          <div class="col-lg-12">
          {% if create_form %}
            <form id="contact_author" role="form" action="{% url 'reports:sendmail' %}" method="post" >
            {% csrf_token %}          
            {{ form.non_field_errors }}
            
            <div class="form-group">
             <div class="row">
             <div class="col-lg-2">
               <label for="{{ form.subject.id_for_label }}">主题:</label>
             </div>
             <div class="col-lg-6">
                {{ form.subject }}
             </div>
             <div class="col-lg-4">
                {{ form.subject.errors }}
             </div>
             </div>
           </div>
            
            <div class="form-group">
             <div class="row">
             <div class="col-lg-2">
               <label for="{{ form.message.id_for_label }}">你的建议:</label>
             </div>
             <div class="col-lg-8">
                {{form.message}}
             </div>
             <div class="col-lg-2">
                {{ form.message.errors }}
             </div>
             </div>
           </div>
           
           <div class="form-group">
             <div class="row">
             <div class="col-lg-2">
               <label for="{{ form.sender.id_for_label }}">你的邮箱:</label>
             </div>
             <div class="col-lg-6">
                {{ form.sender }}
             </div>
             <div class="col-lg-4">
               {{ form.sender.errors }}
             </div>
             </div>
           </div>
           
           <div class="form-group">
             <div class="row"><div class="col-lg-12">
             {{ form.cc_myself.errors }}
             </div></div>
             <div class="row">
             <div class="col-lg-2">
               <label for="{{ form.cc_myself.id_for_label }}">抄送给自己?</label>
             </div>
             <div class="col-lg-6">
                {{ form.cc_myself }}
             </div>
             </div>
           </div>
            
            <div class="row">
              <div class="col-lg-1 col-md-offset-8" >
                <a class="btn btn-danger" href="{% url 'reports:index' %}">取消</a>
              </div>
              <div class="col-lg-1" >
                <button type="submit" value="提交" class="btn btn-success">提交</button>
              </div>
            </div>
            </form>
          {% else %}
            {% if is_success %}
            <p>邮件发送成功啦!请耐心等待小编处理您的邮件!</p>
            {% else %}
            <p>邮件发送失败了....小编也不知道怎么了....</p>
            <p>{{error_message}}</p>
            {% endif %}
            <div class="row"><div class="col-lg-1 col-md-offset-8" >
              <a class="btn btn-danger" href="{% url 'reports:index' %}">确定</a>
            </div></div>
          {% endif %}
            <br><br><br><br><br><br><br><br><br>
          </div>
          </div>
      </div>
  </body>
</html>
